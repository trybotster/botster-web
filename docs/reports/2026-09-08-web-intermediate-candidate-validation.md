# Web intermediate candidate validation

Status: intermediate candidate validation, not acceptance. Final pins remain pending. The generated candidate artifacts remain uncommitted.

The Web base was `c172311d3e3f4fdf22ef80f34d9db7df613b7ffa`. The candidate used Hub `50fc2f2c639bb2d241f4c2bf667d6eff043440ca`, Core `ca243281bc21adb30f13913d38669bbad27083a8`, and Ghostty `eb72ec61304ea256be1d86ed8fa961c84e43ecbd`.

The Hub binary SHA-256 was `eed356c78f0c56cd3b6553b764356aeeee92583ccadc044a14d738cf9377b31b`. The worker binary SHA-256 was `5864d582647bd64169ab558e0c09d81dd85d94c693f60648125f89a5de74145f`. The candidate manifest SHA-256 was `0f965b6b56f3acbb9585fda78e1fd67f5ebecc882ed6af01ef8607e76eb55215`. The exported support manifest SHA-256 was `371bbfb958920ea14ff8dd08c5e0fbea87b3edfbdb684e98a08116fa14ac2271`.

The source-current positive run passed W-S1 through W-S5. W-S1 proved attach, key input, output, and resize. W-S2 proved two concurrent clients on one session. W-S3 proved two 65,536-byte pastes, zero-write unsafe rejection, a new confirmed operation, cancellation, transcript exclusion, and later input progress. Cancellation sent two focus operations. It sent zero paste operations and zero non-focus operations. The two focus operations are the expected blur to the Cancel control and restore to the terminal. Focus frames carry state only. They do not write their one-byte body to the PTY unless Core focus reporting is active. W-S4 proved detach, a new attach, restored visible state, live output, and one abandoned outstanding operation. W-S5 proved a new subscription, a different route generation, restored snapshot, live output, and input round trip. Reconnect took 1,709 ms against the 15,000 ms bound.

The source-current positive log is `smoke-positive-source-current.log`, SHA-256 `b9e75611da66b393926fff23a73682139372c89ec576180275c9af11d81ebf94`. The earlier verified positive log is `smoke-positive-final.log`, SHA-256 `e73273fd43caee0355148518c792a138c7f7e828f1fa64bea29b19c34480cee3`. That earlier log predates the stronger cancellation assertion and the failure-only transition diagnostic. It passed the same product scenarios.

The authoritative reconnect control is `smoke-negative-reconnect-close-source-current.log`, SHA-256 `69198913b73bd69b38eef33e8ec416a3d5d09a3c1aa1ec9d0488d321e8b470a2`. It exited 0 against the source-current harness. It passed W-S1 through W-S4. It identified `reconnect_close_ablated=true`. It observed the expected `ws5-reattach-negative-control` failure after 10,018 ms against the 10,000 ms inner bound. It then printed the negative-control pass line.

The earlier successful control is `smoke-negative-reconnect-close-asserted.log`, SHA-256 `c4d80282c7437bcf2c1f1e7966c59188ccafae888ab0bc99e2d65b52b13833a0`. It exited 0. It predates the stronger W-S3 cancellation assertion and the final failure diagnostic. Keep this log as prior control evidence. Do not use it as the authoritative control result.

The first control log, `smoke-negative-reconnect-close.log`, SHA-256 `996861da5c3ef645ddc9875a06d22bed4b5b996f600228c7903a1555dacccadd`, exited 1. A failure diagnostic appended output text to an error message. The control matched that message by exact equality. Thus, the diagnostic prevented the control from asserting itself. The harness now preserves the original error in `Error.cause`. The control compares that original error. Keep the first log as evidence of this N3 defect. Do not use it as the control result.

One later positive attempt did not reach the N4 cancellation measurement. `smoke-positive-final-n4.log`, SHA-256 `f8613c97849f21188ddc250ccbf8021d1044a88bb45398320f0024edd5f4f3c3`, recorded a mid-W-S3 terminal subscription close, reader cancellation, reattach, and missing receiver-ready output. Hub stderr was empty. The failure record showed generation `3`, but this was the browser-local attachment attempt after route generation cleared. It was not evidence of a Hub allocator reset or worker restart. The reason for the close remains unknown. The later reproduction did not repeat the close. A passing later run does not explain the event. The harness now retains a bounded, failure-only transition tail with raw field presence and separate Hub route identity. It reads that tail only after failure.

The diagnostic reproduction, `smoke-positive-transition-diagnostic.log`, SHA-256 `80f554830a39dce6ac849ba12b1b801bfc83fa3fdcc69459efa554f37b0eb38a`, reached cancellation. It proved that cancellation sent two focus operations. It failed the temporary zero-or-one bound. The final oracle has no sample-derived limit on focus operations. It requires zero paste operations. It also requires the total operation change to equal the focus operation change. Thus, any key, resize, paste, or other input operation fails the cancellation proof.

The static gates ran sequentially after the final harness changes. Syntax checks and `git diff --check` exited 0. `npm test` exited 0 with two existing React `act` warnings. `npm run typecheck` exited 0. `npm run lint` exited 0 with five existing warnings. `npm run build` exited 0 with the existing chunk-size warning. An earlier concurrent gate run exposed an order-sensitive reconnect test failure: the test expected `apps` but received `status`. Isolated runs passed. This N6 result suggests a remaining real-scheduling dependency in a suite that substitutes window timers. This lane did not investigate that test issue.

Known limits remain:

- W-S3 chooses its unbracketed expected digest before its immediate mode read. W-S1 had already read authoritative mode and required `bracketed_paste=false`. W-S3 also checks the mode after the write. The next W-S3 change should use an immediate pre-paste mode observation.
- `scripts/live-packaged-protocol-harness.mjs:7786` still treats an unbracketed wire as unchanged payload. Its CRLF case at line 7904 conflicts with the pinned Ghostty rule. Correct that sibling lane when it is next changed.
- W-S5 assumes that the Hub and Core worker remain active while it compares route generations. The unexplained close does not prove a worker restart. A future failure diagnostic must keep browser-local attachment attempts separate from Hub route generations.
- The earlier LF transcript marker could not match normal CRLF output. Thus, no earlier intermediate lane description proved cancelled transcript exclusion. Only the corrected CRLF marker and the current positive logs prove it.
