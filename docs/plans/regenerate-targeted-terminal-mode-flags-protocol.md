# Regenerate targeted terminal ModeFlags protocol

## Context loaded

- Pipeline run `run_1784580441_429380`, ticket `ticket_1784566295_552369`, its Plan gate, empty prior artifacts/reviews/findings, and the answered release question were loaded through Project Pipelines.
- The original dependency, hub ticket `ticket_1784566294_391171`, merged as botster-hub PR #149 at `06f1fa7`. Its authoritative generated delta is limited to `read_mode_flags`, optional `mode_flags`, `DaemonModeFlags { session_id, mouse_mode }`, and the `read_mode_flags` response kind.
- Release dependency `ticket_1784580694_249720` is closed after publishing `@trybotster/hub-test-support@0.1.8` with conformance revision 15; npm independently resolves `0.1.8` as latest.
- Vault authority: [[planner-playbook]], [[botster-planner-playbook]], [[botster-architecture]], [[cli-patterns]], [[spa-patterns]], [[botster web generated protocol drift checks need explicit hub artifact paths]], [[hub test support npm releases need external consumer smoke]], [[generated typescript dtos must encode serde field optionality]], [[botster web dto field names must match authoritative rust serde structs]], [[blocking dependency premises must be revalidated per consuming crate]], and [[vault example paths are not repository placement conventions]].
- Repository authority: `README.md`, `package.json`, `scripts/check-daemon-protocol-drift.mjs`, `src/botster/generated/daemon-protocol.ts`, `src/botster/realHubDaemonDto.ts`, `src/botster/__fixtures__/generatedDaemonProtocol.ts`, `src/App.test.mjs`, and existing `docs/plans/` prior art.
- Botster layer touched: generated browser DTO/conformance boundary in the React SPA repository. No plugin, Lua core, Rust hub, session worker, TUI, Rails relay, MCP, or UI behavior changes.
- Worktree/target: remain in this assigned botster-web ticket worktree and target `tgt_40abcf71ccf049f4ac0c99953a799869`. The authoritative upstream is merged hub commit `06f1fa7`; implementation must not edit a sibling checkout.

## Scope

1. Pin `@trybotster/hub-test-support` exactly to published version `0.1.8` in `package.json` and `package-lock.json`. Verify the installed package metadata and assets before copying anything.
2. Replace `src/botster/generated/daemon-protocol.ts` byte-for-byte from the installed package artifact. The expected source delta is only:
   - `DaemonRequest` gains `{ type: "read_mode_flags"; session_id: string }`;
   - `DaemonResponse` gains optional `mode_flags?: DaemonModeFlags | null`;
   - `DaemonModeFlags` carries `session_id: string` and `mouse_mode: number` (the TypeScript representation of Rust `u8`);
   - `DaemonResponseKind` gains `"read_mode_flags"`.
3. Extend the existing generated-protocol fixture/test seam only enough to prove compile-time and runtime recognition: include the targeted request and a `satisfies DaemonResponse` response fixture with the new kind, session attribution, and numeric mouse mode. Consume the package's revision-15 mode-flags conformance fixture in `src/App.test.mjs` to assert mouse-off `0`, mouse-on `9`, unknown-session error, backend error, and null failure payloads.
4. Update the README and architecture documentation's pinned package/revision statements to the published version and revision 15, while stating that ModeFlags is generated protocol scaffolding and has no web UI consumer yet. Preserve the existing revision-14 terminal attachment/runtime requirement, and bind both documents to the pinned package version/revision in the existing test seam.

This is intentionally scaffold-only at runtime. The production type entry point is `src/botster/realHubDaemonDto.ts`, which re-exports the generated DTOs used by browser daemon adapters; this ticket makes the response representable there but deliberately adds no request invocation, polling, or rendering path.

## Non-scope

- No UI, mouse-mode indicator, terminal routing, polling, request helper, pushed event, connection diagnostic, or capability requirement.
- No change to `minimumConformanceFixtureRevision` or existing revision-14 terminal-readback compatibility behavior.
- No hand-written parallel protocol types, browser-only discriminator, fixture fork, compatibility branch, or fallback to revision 14.
- No hub/core/session-worker/TUI/plugin changes, package publication from this repo, unrelated dependency upgrades, generated churn, or adjacent cleanup.

## Assumptions and unknowns

- Binding human decision: source-exact regeneration and green default package-backed gates are both required; source-only verification is not an acceptable waiver.
- Verified precondition: npm resolves immutable package version `0.1.8`; the closed release ticket records conformance revision 15, the ModeFlags fixture/export, and generated bytes derived from merged PR #149.
- Assumption: TypeScript continues to represent Rust `u8` as `number`; no browser-side range abstraction is required for a generated DTO.
- Assumption: PR #149's generated eight-line delta remains the complete intended protocol change. If the published artifact contains other DTO changes, stop and reconcile them rather than accepting unrelated generated churn.
- Convention conflicts: none after adding the release dependency. The initial package/source conflict was resolved by the human answer and blocking publish ticket.

## Affected surfaces/files

- `package.json`, `package-lock.json`: exact published hub-test-support pin.
- `src/botster/generated/daemon-protocol.ts`: byte-for-byte generated artifact replacement.
- `src/botster/__fixtures__/generatedDaemonProtocol.ts`: narrow typed request/response recognition fixtures; no duplicate type declarations.
- `src/App.test.mjs`: package metadata/revision, protocol tokens, conformance scenarios, and typed fixture assertions.
- `README.md`: current package/revision statement plus explicit scaffold-only disposition.
- `docs/architecture.md`: current published package/revision statement while preserving the minimum revision-14 compatibility policy.
- `src/botster/realHubDaemonDto.ts`: unchanged production re-export seam that proves the generated surface is wired into the browser client type boundary.
- `scripts/check-daemon-protocol-drift.mjs`: expected unchanged; it already fails hard against the installed authoritative package and accepts an explicit source artifact for verification.

## Risks

- Publishing/version collision: reusing `0.1.7` for revision-15 bytes would make immutable registry verification impossible. Require a new exact package version.
- Split authority: copying from merged source while retaining the revision-14 npm pin makes default `npm test` red. The publish dependency and exact pin prevent that state.
- Unrelated generated churn: a later package could include additional protocol changes. Compare installed package bytes with merged hub-client output and reject unexplained differences.
- Weak recognition proof: string matching alone would show text exists but not that `DaemonResponse` accepts the payload. Keep a typed `satisfies DaemonResponse` fixture plus conformance assertions.
- Accidental product behavior: raising the browser's minimum conformance revision or adding a request consumer would broaden a scaffold-only ticket. Assert those surfaces remain untouched.
- Self-authored fixture drift: use the package's authoritative mode-flags fixture for semantic values and errors; keep the local fixture limited to TypeScript compilation/recognition.

## Acceptance checks/tests

Precondition evidence:

- Registry resolves the new exact package version, installed metadata reports conformance revision 15, `verifyPackageAssets()` passes, and the release ticket's clean external-consumer smoke proves the new protocol tokens and fixture export.

Implementation checks:

1. Install the exact release and confirm `package.json` and lockfile resolve only that version.
2. Compare `src/botster/generated/daemon-protocol.ts` byte-for-byte with the installed package artifact and require SHA-256 `5b13c8f337d7cf420aa8badce34d3d469e28ef5d1e7536068ba1cb4be6eb55a7`, matching the package metadata validated by `verifyPackageAssets()`. When `~/Projects/botster-hub` is available, also materialize `06f1fa7:crates/botster-hub-client/generated/daemon-protocol.ts` and compare it byte-for-byte; if that external checkout is absent, record the corroborating source comparison as skipped with the reason rather than weakening the mandatory package-backed proof.
3. Run `npm test`. This must prove the default package-backed drift check, package metadata/assets, revision-15 conformance scenarios, runtime request/response fixture assertions, and existing web regression suite without overrides. A green `npm test` alone does not prove that `DaemonResponse` accepts the typed ModeFlags fixture.
4. When the merged hub-client source artifact is available, run the drift check and full test again with `BOTSTER_HUB_CLIENT_DAEMON_PROTOCOL` pointing to it; no skip or drift warning is acceptable for a supplied path.
5. Run `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check`. `npm run typecheck` and `npm run build` are the mandatory, non-waivable compile-time proof for the `satisfies DaemonResponse` fixture.
6. Inspect the final diff: generated protocol changes must match the four expected generated additions, package/lock changes must be limited to the exact release, and no UI/runtime consumer or unrelated protocol changes may appear.

No live browser or hub smoke is required: the ticket intentionally changes only generated representability and conformance recognition, not a runtime request path. Existing repo gates remain mandatory.

## Pipeline gates and artifacts

- Resolved precondition: release ticket `ticket_1784580694_249720` is closed and npm resolves `@trybotster/hub-test-support@0.1.8`.
- Plan artifact: this document plus the `botster_plan_gate` evidence.
- Implement artifact must record the exact package version, conformance revision, upstream merge commit, changed files, byte comparisons, commands/results, and confirmation that no UI/runtime consumer was added.
- Review must reject a source-only drift run, a red/default-skipped package gate, revision reuse, unrelated generated changes, or any accidental runtime behavior.
- Verify must rerun both package-backed and explicit-source drift paths and the repo gates from the implementation commit.
- No plugin README update is relevant because no plugin surface changes.

## Vault gaps worth capturing

No new durable vault gap. The discovered release seam and its required external-consumer proof are already covered by [[hub test support npm releases need external consumer smoke]] and [[botster web generated protocol drift checks need explicit hub artifact paths]]. The pipeline-specific action is preserved as the new blocking release ticket rather than duplicated into another note.
