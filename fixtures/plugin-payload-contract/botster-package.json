{
  "name": "botster.plugin-payload-contract",
  "version": "1.0.0",
  "kind": "plugin",
  "botster": ">=0.1.0",
  "source": { "type": "path", "path": "." },
  "capabilities": [
    { "surface": "surfaces" }
  ],
  "entrypoints": [
    { "runtime": "lua", "path": "plugin.lua", "bootstrap": false }
  ],
  "surfaces": [
    {
      "id": "payload.app",
      "kind": "app",
      "title": "Payload Contract",
      "description": "Conformance app surface proving UiAction payload delivery.",
      "icon": "workflow",
      "order": 10,
      "category": "contract",
      "supports": ["render", "action"]
    }
  ]
}
